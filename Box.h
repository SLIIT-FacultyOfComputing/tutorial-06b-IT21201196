class Box {
    private:
       int length;
       int width;
       int height;
    public:
      void setLength(int n1);
      void setWidth(int n2);
      void setHeight(int n3);
      int getLength();
      int getWidth();
      int getHeight();
      
       // write prototypes of setters for length, width and height
       // write prototypes of getters for length, width and height
 
       int calcVolume();
};
